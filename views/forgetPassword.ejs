<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Forgot Password - JustVkey - Be JUST You Everyday</title>
  <link rel="icon" href="/assets/images/logo/favicon.png" type="image/x-icon" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link rel="stylesheet" href="/assets/styles/main.css" />
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" rel="stylesheet" />

  <style>
    .auth-page {
      min-height: 100vh;
      background: linear-gradient(135deg, var(--off-white), var(--pure-white));
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 2rem;
    }

    .auth-card {
      background: var(--pure-white);
      border-radius: 24px;
      box-shadow: 0 20px 60px rgba(0, 0, 0, 0.1);
      border: 1px solid var(--medium-gray);
      padding: 3rem;
      max-width: 500px;
      width: 100%;
      position: relative;
    }

    .auth-card::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      height: 4px;
      background: linear-gradient(135deg, var(--accent-sage), var(--accent-blush));
    }

    .auth-logo {
      width: 80px;
      height: 80px;
      border-radius: 50%;
      border: 3px solid var(--accent-sage);
      background: var(--pure-white);
      padding: 8px;
      margin: 0 auto 1rem;
      display: block;
      transition: all 0.3s ease;
    }

    .auth-title {
      font-family: 'Playfair Display', serif;
      font-size: 1.6rem;
      font-weight: 600;
      color: var(--primary-black);
      text-align: center;
      margin-bottom: 1.5rem;
      position: relative;
    }

    .auth-title::after {
      content: '';
      position: absolute;
      bottom: -8px;
      left: 50%;
      transform: translateX(-50%);
      width: 50px;
      height: 2px;
      background: linear-gradient(135deg, var(--accent-sage), var(--accent-blush));
      border-radius: 2px;
    }

    .form-floating {
      margin-bottom: 1.2rem;
    }

    .form-control {
      border: 2px solid var(--medium-gray);
      border-radius: 16px;
      background-color: var(--off-white);
      padding: 1rem;
      transition: all 0.3s ease;
      font-size: 1rem;
    }

    .form-control:focus {
      border-color: var(--accent-sage);
      background-color: var(--pure-white);
      box-shadow: 0 0 0 3px rgba(212, 175, 55, 0.1);
    }

    .btn-submit {
      background: linear-gradient(135deg, var(--accent-sage), #b8941f);
      color: var(--pure-white);
      border: none;
      padding: 0.75rem;
      font-size: 1.1rem;
      font-weight: 600;
      border-radius: 16px;
      width: 100%;
      transition: all 0.4s ease;
      box-shadow: 0 8px 25px rgba(212, 175, 55, 0.3);
    }

    .btn-submit:hover {
      transform: translateY(-2px);
      box-shadow: 0 15px 35px rgba(212, 175, 55, 0.4);
    }

    .alert {
      border-radius: 16px;
      border: none;
      font-weight: 500;
      margin-bottom: 0;
      position: fixed;
      top: 20px;
      left: 50%;
      transform: translateX(-50%);
      z-index: 1050;
      min-width: 300px;
      text-align: center;
    }

    .alert-success {
      background: linear-gradient(135deg, var(--accent-sage), #b8941f);
      color: var(--pure-white);
      box-shadow: 0 10px 30px rgba(212, 175, 55, 0.3);
    }

    .alert-success .btn-close {
      filter: invert(1);
    }

    @media (max-width: 576px) {
      .auth-card {
        padding: 2rem;
      }
    }
  </style>
</head>

<body>
  <% if (message) { %>
    <div class="alert alert-success alert-dismissible fade show" role="alert">
      <i class="fas fa-check-circle me-2"></i>
      <%= message %>
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    </div>
    <% } %>

      <div class="auth-page">
        <div class="auth-card fade-in-up">
          <a href="/">
            <img src="/assets/images/logo/logo2.jpg" alt="JustVkey Logo" class="auth-logo" />
          </a>
          <h2 class="auth-title">Forgot Password</h2>

          <form action="/forgot-password" method="POST">
            <div class="form-floating">
              <input type="email" class="form-control" id="email" name="email" placeholder="Enter your email"
                required />
              <label for="email"><i class="fas fa-envelope me-2"></i>Email Address</label>
            </div>

            <button type="submit" class="btn-submit mt-3">
              <i class="fas fa-paper-plane me-2"></i>Send Reset Link
            </button>
          </form>

          <div class="text-center mt-4">
            <a href="/login" class="text-decoration-none fw-bold" style="color: var(--accent-sage);">Back to Login</a>
          </div>
        </div>
      </div>

      <!-- Bootstrap JS -->
      <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>